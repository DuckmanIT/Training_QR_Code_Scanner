<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.QR_Test" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.QR_Test" parent="Base.Theme.QR_Test" />
    <style name="Custom_Dialog" parent="ThemeOverlay.AppCompat.Light" >
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.5</item>
        <item name="windowMinWidthMajor">100%</item>
        <item name="windowMinWidthMinor">65%</item>
    </style>

    <style name="DatePicker_Dialog" parent="ThemeOverlay.AppCompat.Dialog" >
        <item name="colorAccent">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary</item>
        <item name="colorOnPrimary">@color/primary</item>
        <item name="android:textColorPrimary">#000000</item>
    </style>
</resources>