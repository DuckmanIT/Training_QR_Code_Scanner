<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:background="@color/backGround"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">


    <ImageView
        android:id="@+id/qr_icon"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/icon"
        android:paddingTop="184dp"
        app:layout_constraintTop_toTopOf="parent"
        />

    <TextView
        android:id="@+id/qr_code_scanner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/qr_code_scanner"
        android:layout_marginTop="24dp"
        android:fontFamily="@font/inter_bold"
        android:textSize="24dp"
        android:textColor="@color/blackIcon"
        app:layout_constraintTop_toBottomOf="@+id/qr_icon"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        />

    <TextView
        android:id="@+id/this_action_maybe_contains_ads"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/this_action_maybe_contains_ads"
        android:fontFamily="@font/inter_regular"
        android:textSize="14dp"
        android:textColor="@color/neutral"
        android:layout_marginTop="380dp"
        app:layout_constraintTop_toBottomOf="@+id/qr_code_scanner"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        />


</androidx.constraintlayout.widget.ConstraintLayout>